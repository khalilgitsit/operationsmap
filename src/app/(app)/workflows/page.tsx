import { PageHeader } from '@/components/page-header';

export default function WorkflowsPage() {
  return (
    <div>
      <PageHeader title="All Workflows" />
      <p className="text-muted-foreground">Workflow list will be built in a later phase.</p>
    </div>
  );
}
