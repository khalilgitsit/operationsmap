# Ops Map — Function Chart YAML Template
#
# Required fields are marked with (REQUIRED).
# Optional fields are shown as comments — uncomment and fill in as needed.
# All imported items will be created with Draft status.
#
# Structure: functions → subfunctions → core_activities

functions:                     # (REQUIRED) At least 1 function
  - title: ""                  # (REQUIRED) Name of this function/department
    # description: ""          # What this function is responsible for
    subfunctions:
      - title: ""              # (REQUIRED) Name of this subfunction/team
        # description: ""      # What this subfunction handles
        core_activities:
          - title: ""          # (REQUIRED) Name of this activity
            # trigger: ""      # What initiates this activity
            # end_state: ""    # What the completed state looks like
